import { StyleSheet } from "react-native";
import { COLOR } from "../../config";

export default StyleSheet.create({
  header: {
    width: "100%",
    marginBottom: 15,
    color: COLOR.GOLD,
    fontVariant: "italic",
    fontWeight: "bold",
    fontSize: "1.5em",
    textAlign: "center"
  }
});
